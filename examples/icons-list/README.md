This elm-simple-icons example displays all icons in a grid.

To view it, run the following:

```sh
elm reactor
```

Then visit the URL that it displays, and select `Main.elm`.

You can press `Ctrl+C` on your terminal to stop reactor.
